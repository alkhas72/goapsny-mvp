# П-22 — evidence: один вход, сайт без Telegram

Дата: 2026-07-21
Ветка: `feat/epoch3-g2-pwa-c1`
База: `052296f` (origin/main после PR #8, #9)

## Что сделано

Удалено:
- `public.html` (16 строк)
- `src/public-main.tsx` (14 строк)

Изменено:
- `vite.config.ts` — снят `rollupOptions.input` со второго входа. Теперь дефолт: один `index.html`.

`diff --stat`:
```
 public.html         | 16 ----------------
 src/public-main.tsx | 14 -------------
 vite.config.ts      | 13 ++++---------
 3 files changed, 4 insertions(+), 39 deletions(-)
```

## Доказательство: двойного бандла нет

### Build ДО (baseline)
```
dist/public.html                       0.80 kB │ gzip:   0.49 kB
dist/index.html                        1.36 kB │ gzip:   0.77 kB
dist/assets/registerSw-CK7FpEIX.css   38.81 kB │ gzip:  11.11 kB
dist/assets/public-DY3s7Z_m.js         0.21 kB │ gzip:   0.19 kB
dist/assets/main-DQLNHbCV.js          46.04 kB │ gzip:  13.81 kB
dist/assets/registerSw-B4pvKZXN.js   598.90 kB │ gzip: 173.48 kB
```

### Build ПОСЛЕ
```
dist/index.html                   1.27 kB │ gzip:   0.74 kB
dist/assets/index-CK7FpEIX.css   38.81 kB │ gzip:  11.11 kB
dist/assets/index-4lsovVTh.js   644.78 kB │ gzip: 185.74 kB
```

`dist/public.html` и `dist/assets/public-*.js` больше не появляются. Один `index.html`, одна точка входа, один JS-чанк.

## Доказательство: сайт живёт без Telegram

### browser-smoke (26/26 PASS)
Команда: `BROWSER_SMOKE_URL=http://localhost:4173/ npm run browser-smoke`
Скрипт: `scripts/browser-smoke.mjs:106-109` блокирует `**://telegram.org/**` через `context.route(... abort())` и считает запросы.

Ключевые гейты:
```
[PASS] browser: preview reachable: http://localhost:4173/
[PASS] browser: public entry does not depend on telegram.org: запросов нет
[PASS] browser: service worker registered
[PASS] browser: manifest linked: /manifest.webmanifest
[PASS] browser: user location shown automatically on map open
[PASS] browser: console/page errors: none
=== BROWSER SUMMARY: 26/26 PASS ===
```

Полный лог: `ais-artifacts/evidence/browser-smoke-2026-07-21.log`

### Код-ревью: синхронного `<script>` с telegram.org нет

- `index.html` — упоминание `telegram.org` только в HTML-комментарии (строки 11-17), без тега `<script>`. Подтверждено: `rg "telegram\.org|telegram-web-app" index.html public/` → 1 совпадение, в комментарии.
- `src/main.tsx:14-27` — bootstrap вызывает `ensureTelegramSdk()`, который внутри (`src/utils/telegramSdk.ts:48-52`) сначала проверяет `looksLikeTelegramLaunch(window.location)` (параметр `tgWebApp` в hash/search). Если запуск не из Telegram — SDK вообще не запрашивается. Если из Telegram — грузится с `async` и таймаутом 5с, после которого страница рисуется без него.
- `src/App.tsx:15-24` — детект через `window.Telegram?.WebApp?.initData`. Нет initData → `PublicApp`. Любая ошибка чтения → catch → тоже `PublicApp`.

Цепочка зависимостей от Telegram разорвана на трех уровнях: `<head>`, загрузка SDK, выбор компонента.

## Доказательство: тесты не regress'нули

- `npm test`: 17 файлов, 157/157 PASS (как в baseline).
- `npm run build`: успешно, без двойного бандла.
- `npm run lint`: 9 ошибок + 2 предупреждения — все предсуществующие (`react-hooks/exhaustive-deps`, `no-explicit-any`), не от моих правок. Я добавил только комментарии в `vite.config.ts`.

## PWA-инфраструктура для установки

- `public/manifest.webmanifest`: `start_url: "/"`, `scope: "/"`, `display: "standalone"`, иконки 192/512, theme/background цвета заданы.
- `public/sw.js`: честный network-only сервис-воркер, перехватывает `fetch` (требование installability).
- `src/registerSw.ts`: регистрируется на `load`, только если `'serviceWorker' in navigator`.
- `src/main.tsx:18` — SW НЕ регистрируется в Telegram WebView (там он не нужен и мешает).

## Про +2 теста к baseline 20.07 (отдельный гейт)

Baseline 20.07 (на `48604af`) = 155 тестов. Текущая база worktree = `052296f` = 157 тестов (+2). Бриф явно сказал брать `052296f` (main ушёл вперёд на PR #8 и #9). Я тестов не писал — diff attribution:

- `+1` в `src/components/PublicMap.test.tsx` (12 → 13):
  - `leads from cabinet login straight to the add form` — commit `f996a76` (PR #8, `fix(public): вход через кабинет ведёт к форме добавления`).
- `+1` в `tests/contract/t2-roles-rls.contract.test.ts` (56 → 57):
  - `submission limit is dropped by a later migration (Арбитр, 20.07)` — commit `67f04c6` (PR #9, `feat(db): снять лимит одной публичной подачи`). В этом же коммите добавляется ещё один `it` (`T2 introduced a one-submission-per-contributor limit`) и удаляется старый — чистый +1 по счету.

Команда для самопроверки:
```
git log --oneline -S "<имя теста>" 48604af..052296f -- <путь>
```
Никаких тестов я не трогал; `diff --stat` моего коммита `54d8c3d` — только `public.html`, `src/public-main.tsx`, `vite.config.ts`, плюс документы.

## Не в границах партии (зафиксировано, не правилось)

- `apple-touch-icon` для iOS не подключён в `index.html`. На iOS "Добавить на экран «Домой»" работает и без него, но иконка будет скриншотом страницы. Не регрессия — так было в baseline.
- Существующие дефекты от прохода 20.07 (DG-3 — успех при отказе сервера; метка появляется только после перезагрузки) — вне скоупа П-22, оставлены как есть.

## Локальные артефакты (gitignored)

- `browser-smoke-2026-07-21.log` — полный вывод `browser-smoke` (26/26 PASS).
- `build-2026-07-21.log` — полный вывод `vite build` (один бандл).

Воспроизводимы командами `BROWSER_SMOKE_URL=http://localhost:4173/ npm run browser-smoke` и `npm run build` на коммите `54d8c3d`.
