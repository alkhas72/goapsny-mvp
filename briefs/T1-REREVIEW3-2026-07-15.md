# T1 immutable re-review 3 — public PWA map

Reviewed by: SOL  
Reviewed at: 2026-07-15 21:53 MSK  
Immutable source: local HEAD and `origin/feat/pwa-public` at `6bf008e940244121736141c40fe7a49aa96e18f2`  
Mode: `superpowers-lite` — immutable review, local verification, and documentation only; no product edit, DB/migration/fixture apply, live DB/Storage smoke, deploy, secret/webhook mutation, external message, PR, or merge  
Verdict: **CHANGES REQUIRED — T1 is not clean for G1**  
Test-DB verdict: **HOLD — not READY-FOR-APPLY**

## Immutable pin and fresh verification

- T1 worktree was clean; local HEAD and local `origin/feat/pwa-public` both resolved to `6bf008e940244121736141c40fe7a49aa96e18f2` before review. No dirty T1 state was reviewed.
- `npm test`: **PASS**, 27/27 tests in 6 files.
- `npm run build`: **PASS**; both `dist/index.html` and `dist/public.html` were produced.
- `npm run lint`: **FAIL**, the established 9 errors + 2 warnings in preserved Mini App files `AddWizard.tsx`, `services/api.ts`, and `utils/telegram.ts`. No new finding in the three round-2 correction areas was reported; the repository-wide lint gate is nevertheless not green.
- `npm run smoke:gate-offline`: **PASS**, 3/3.
- `npm run smoke:harness-offline`: **PASS**, 22/22. This is structural/predicate evidence, not a substitute for the Arbiter-gated applied live smoke.
- Fresh deterministic browser smoke against a local build at `/public.html`: **PASS**, 22/22, including real Chromium keyboard Enter/Tab/Shift+Tab/Escape on a real Leaflet marker and focus return to the current remounted marker.
- `git diff --check` and final T1 worktree status were clean. No apply or deploy was performed.

## Round-2 OPEN-block disposition

### 1. DB/Storage smoke pending denial, Storage denial, and private bucket — **OPEN (`064e999`)**

Commit `064e9999c004833c8584ed0b0af67c8b5b02a776` closes most of the requested harness surface:

- `scripts/smoke-public.mjs:235-277` now checks hidden and pending facade metadata denial and signed-URL denial.
- `scripts/smoke-public.mjs:120-129` verifies through the admin client that `place-photos.public === false`.
- `scripts/smoke-public.mjs:138-146` checks anonymous Storage upload denial.
- The deterministic pending/hidden/published fixture paths are required before the positive/negative DB proofs continue.

One blocking contradiction remains:

- `scripts/smoke-public.mjs:131-136` calls anonymous `list('', { limit: 1 })`, while `scripts/lib/smoke-storage-proofs.mjs:27-30` treats only an error or a globally empty root result as “list denied”.
- `supabase/migrations/20260714141000_public_read_published.sql:37-49` intentionally grants anon `SELECT` on published-backed `storage.objects` so the public client can create signed URLs for published photos. Supabase Storage listing requires the same object `SELECT` permission. With the published deterministic objects applied, a root listing may legitimately expose a published folder/object; the current smoke can therefore fail after a correct apply.
- This must not be deferred to live apply. The harness should prove that known pending and hidden prefixes/objects are absent to anon while allowing the published-backed objects authorized by policy. Admin should first verify that all denied fixture objects physically exist. The private-bucket and anonymous upload-denial assertions should remain.

Because the accepted fail-closed gate is internally inconsistent with the reviewed policy, block 1 remains **OPEN** and the test-DB verdict remains **HOLD**. Applying the current fixture set would not resolve the code defect.

### 2. Current-marker focus and real keyboard path — **CLOSED (`9f5ca79`, `6bf008e`)**

- `9f5ca796c90fdfc41a802edc3a117687bbee672d` registers Leaflet marker add/remove handlers before `addTo`, refreshes the live marker-button map through remount, and stores only the pending place ID for focus restoration.
- `6bf008e940244121736141c40fe7a49aa96e18f2` removes the obsolete detached-element focus plumbing.
- `scripts/browser-smoke.mjs:184-214` exercises keyboard marker activation, place-sheet Tab and Shift+Tab containment, Escape, and focus on the current visible marker after Leaflet remount. Fresh result: **PASS 22/22**.

### 3. Controlled error -> retry -> success — **CLOSED (`ec19f69`)**

- `ec19f69f9b7af38b148964a2d6a8832f963454ba` replaces full-page reload with an in-process retry attempt and adds controlled integration coverage.
- `src/components/PublicMap.test.tsx:143-153` is non-vacuous: the first published-place request rejects, the retry is activated, the second request resolves, and a returned marker is awaited.
- Non-blocking test-quality note: the additional place-sheet retry test at `src/components/PublicMap.test.tsx:185-195` can find the already-mounted named dialog while it is still in its error/loading state, so its final assertion alone does not prove sheet recovery. This does not reopen the accepted block because the re-review required a controlled public-load **and/or** place-sheet flow and the public-load proof is valid. A future test should assert call count/error disappearance/success content.

## Final status table

| Item | Status |
|---|---|
| Pending-parent facade metadata denial | **CLOSED (`064e999`)** |
| Pending/hidden signed-URL denial | **CLOSED (`064e999`)** |
| Explicit private `place-photos` bucket proof | **CLOSED (`064e999`)** |
| Anonymous Storage upload denial | **CLOSED (`064e999`)** |
| Anonymous Storage list-denial proof consistent with published-read policy | **OPEN (`064e999` partial)** |
| Leaflet marker -> sheet -> current-marker keyboard focus | **CLOSED (`9f5ca79`, `6bf008e`)** |
| Controlled error -> retry -> success | **CLOSED (`ec19f69`)** |
| Test-project migrations/seeds/fixtures apply | **WAITING ON ARBITER, but not eligible while code verdict is HOLD** |
| Live anonymous DB/Storage smoke | **WAITING ON ARBITER, after accepted apply** |
| Preview deploy | **WAITING ON ARBITER** |
| Antigravity screenshots/audit | **WAITING ON ARBITER, after preview** |
| Alkhas physical-device G1 | **WAITING ON ARBITER, after preview/visual evidence** |
| Canonical external URLs | **WAITING ON ARBITER** |

## Exact next action and later Arbiter gates

T1 must first produce one focused pushed correction SHA for the Storage-list proof and return it for immutable SOL re-review. The required proof is: admin confirms the pending/hidden objects exist; anon cannot enumerate the known pending/hidden prefixes/objects; published-backed access remains allowed; private bucket and anonymous upload denial remain asserted.

There is therefore **no READY-FOR-APPLY request to the Arbiter yet**. Once the focused correction is accepted and T1 becomes code-clean, the remaining G1 decisions must be requested explicitly and in this order:

1. Authorize applying the reviewed migrations, seeds, and deterministic fixtures to the isolated test project.
2. Authorize and observe the full live anonymous DB/Storage smoke on that applied test project.
3. Authorize preview deployment of the public PWA entry and required preview configuration.
4. Run Antigravity screenshots/audit against the approved preview.
5. Obtain Alkhas's physical-device G1 verdict.
6. Supply/approve canonical external URLs before enabling those currently disabled controls.

The inherited repository-wide lint debt remains 9 errors + 2 warnings and must stay visible in release reporting. T3 was not inspected or changed in this tact.
