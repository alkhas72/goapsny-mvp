# Composer 3 — preview/PWA/offline evidence lane

You are the sole writer for `feat/epoch3-landing-stitch-composer`. The branch name is historical; your bounded task is preview/PWA evidence, not landing integration.

Read `.ui-craft/brief.md`, `.ui-craft/tokens.md`, `DESIGN.md`, and existing preview/offline scripts. Do not edit application UI, auth, roles, Supabase logic or map behavior.

Create or consolidate a deterministic local evidence harness for:

- dual entrypoints and PWA metadata;
- manifest and service-worker/installability facts without claiming unsupported physical-device behavior;
- offline preview fixtures for public places and marker states;
- console/page errors and stable screenshot setup;
- clear separation of isolated test environment from production.

Reuse proven existing scripts where possible; do not create a parallel application. Run focused checks, full test suite and build. Commit and push only this branch with a concise report.

Start response: `ACK COMPOSER-3 PREVIEW-HARNESS`.

