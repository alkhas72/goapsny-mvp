# T1 immutable re-review 2 — public PWA map

Reviewed by: SOL  
Reviewed at: 2026-07-15 21:24 MSK  
Immutable source: remote `refs/heads/feat/pwa-public` at `865e6a02da16387608a7bef7b02c5d9daa61c543`  
Mode: `superpowers-lite` — read-only code review and orchestration documentation; no product edit, DB/fixture apply, deploy, secret/webhook mutation, external message, PR, or merge  
Verdict: **CHANGES REQUIRED — T1 is not yet clean for G1**  
Test-DB verdict: **HOLD — not READY-FOR-APPLY**

## Immutable pin and independent verification

- `git ls-remote origin refs/heads/feat/pwa-public`, local HEAD, and local `origin/feat/pwa-public` all returned `865e6a02da16387608a7bef7b02c5d9daa61c543`.
- The T1 worktree was clean before and after review; `git diff --check`, unstaged diff, and staged diff were clean. No T1 writer or product file was changed by SOL.
- `npm test`: **PASS**, 24/24 tests in 6 files.
- `npm run build`: **PASS**; both `dist/index.html` (Telegram Mini App) and `dist/public.html` (public PWA) were produced.
- `npm run lint`: **FAIL**, 9 errors + 2 warnings. The findings are confined to the preserved Mini App files `AddWizard.tsx`, `services/api.ts`, and `utils/telegram.ts`; the count matches the prior immutable review and introduces no newly observed correction-lane lint regression.
- `npm run smoke:gate-offline`: **PASS**, 3/3 host-boundary checks.
- `npm run smoke`: **FAIL**, 2/9. The approved test target passed `.env.test.local` and `URL ↔ TEST_SUPABASE_PROJECT_REF` preflight, then failed closed because all seven required fixture checks are absent. No setup/apply was run.
- `npm run browser-smoke` against the freshly built `http://127.0.0.1:4174/public.html`: **PASS**, 18/18. It covered five viewports, menu/filter Enter/Escape/Tab/Shift+Tab, focus return to their triggers, locate-button reachability, service-worker registration, manifest linkage, and zero captured console/page errors.
- Review size against `origin/main`: 49 files, 4,151 insertions and 98 deletions. The correction round is split into additive pushed commits; the large `86fb8a2` block is predominantly the mechanical preservation of the pre-existing Telegram `App.tsx` as `TelegramApp.tsx`.

## Blocking findings

### 1. BLOCKER — the DB/Storage harness still does not satisfy the accepted gate

Commit `8607fbdf17f401b13fd7e29b1b38359ac2e06846` correctly adds stable IDs, an idempotent four-place/four-object setup, exclusive `TEST_SUPABASE_*` loading, and an exact URL-host/project-ref check. Those parts are closed.

The executable proof remains incomplete against `briefs/T1-TESTDB-GATE-2026-07-15.md`:

- `scripts/setup-test-fixtures.mjs:110-120` creates both pending and hidden facade metadata/objects, but `scripts/smoke-public.mjs:34-103,125-143` discovers and requires only the hidden facade path. `pendingFacadePath` is never consumed by the smoke.
- `scripts/smoke-public.mjs:176-232` proves pending/hidden place denial, but facade metadata and signed-URL denial are checked only for the hidden parent. The required pending-parent photo/object denial is absent.
- `scripts/smoke-public.mjs:234-253` checks table writes and profile reads, but never proves anonymous Storage upload/list denial or `place-photos.public=false`, both required by the gate and original T1 contract.
- The live pre-apply run is correctly fail-closed at 2/9, so no non-vacuous DB/Storage result exists yet.

Required before a new DB decision: a focused pushed harness commit covering pending photo metadata/object denial, anonymous Storage list/write denial, and bucket privacy; then SOL re-review. Current verdict remains **HOLD**. No migration or fixture apply is authorized.

### 2. HIGH — place-sheet keyboard focus return is not closed in the real Leaflet path

Commits `70e7de2115c65d3486af05257860d389e7b656f4` and `3db546bd5577dd0200775858defb021d82aa1ba0` add generic focus-trap tests and browser checks for menu and filters, but not the place sheet or a real marker.

- `src/components/PublicMap.tsx:121-154` stores the currently clicked marker element in `lastMarkerRef`.
- `src/components/LeafletMap.tsx:164-201` clears and recreates every marker whenever `selectedPlaceId` changes. The saved element is therefore detached after selection; the replacement marker is registered only in `markerButtonsRef`, not in `lastMarkerRef`.
- `src/components/PublicMap.tsx:226-236` later focuses the stale saved element when the sheet closes. Focusing a detached button does not return keyboard focus to the visible replacement pin.
- `scripts/browser-smoke.mjs:84-138` exercises only menu/filter focus. `PublicMap.test.tsx` uses stable React mock buttons and does not close the place sheet, so it cannot expose the Leaflet remount/stale-ref path.

Required: fix the marker-ref lifecycle and add a controlled integration/browser test for marker keyboard activation plus place-sheet Escape, forward Tab, Shift+Tab, and focus return to the current visible pin.

### 3. HIGH — the required error/retry integration proof remains partial

Commit `3db546bd5577dd0200775858defb021d82aa1ba0` proves initial load, visible status filtering, marker selection, sheet rendering, and partial-photo behavior. It renders the map-load error state, but no test activates `Повторить` and proves recovery; no controlled place-sheet error/retry flow is exercised. The browser smoke never selects a populated marker or opens a sheet.

Required: add a controlled error→retry→success integration for the public load and/or the place sheet as specified by the first re-review, without relying on a live mutable dataset.

## Prior blocker disposition

| Prior finding | Disposition |
|---|---|
| Deterministic fail-closed test DB gate | **OPEN** — `8607fbd` is partial; DB verdict HOLD for the missing pending/Storage proofs above |
| Preserve Telegram Mini App and add public PWA entry | **CLOSED (`86fb8a2`)** — root `index.html` runs `TelegramApp`; `/public.html` runs `PublicApp`; both build |
| Document public client environment names | **CLOSED (`86fb8a2`)** — `.env.example` contains names-only `VITE_SUPABASE_*` and separate test/server blocks |
| Populated-map integration | **OPEN** — `3db546b` closes load/filter/selection/partial-photo, but not error→retry and real sheet keyboard path |
| Focused immutable correction blocks | **CLOSED (`8607fbd`…`865e6a0`)** — additive pushed SHA map, no rewrite |
| Tab/Shift+Tab/Escape/focus return | **OPEN** — menu/filter and generic trap are green; real marker→sheet→marker return remains broken/unproved |
| Filter apply closes and restores focus | **CLOSED (`3db546b`)** |
| Bounded public data load | **CLOSED (`352c046`)** — `.range(0, 499)` via `PUBLIC_PLACES_FETCH_LIMIT = 500` |
| Factual pushed handoff | **CLOSED (`865e6a0`)** — counts and non-runs are stated accurately |
| Browser zoom, visible attribution, Russian-only UI, click-only geolocation, unresolved external links disabled | **CLOSED (`1ceedcc`, retained by `86fb8a2`)** |
| Antigravity screenshots, Alkhas device G1, canonical external URLs, preview deploy, secrets, PR/main merge | **WAITING ON ARBITER** |

## Exact work remaining before G1

1. T1 correction SHA(s) for the complete DB/Storage gate, current-visible-marker focus return, place-sheet keyboard coverage, and controlled error→retry integration; then another immutable SOL re-review.
2. After a future SOL `READY-FOR-APPLY` and explicit Arbiter gate only: apply the reviewed schema/seeds/fixtures to the isolated test project and obtain a full green anonymous DB/Storage smoke. Current apply decision is HOLD.
3. Explicit preview deployment of the public entry (`/public.html` or an approved host rewrite), followed by Antigravity screenshots/audit and Alkhas physical-device verdict. No deploy was performed here.
4. Arbiter-supplied canonical external URLs if those disabled controls are to become active. The inherited full-repository lint debt remains 9 errors + 2 warnings and should stay recorded, but no new correction-lane lint regression was observed.

T3 was not inspected or changed during this review.
