# T1 immutable re-review — public PWA map

Reviewed by: SOL  
Reviewed at: 2026-07-15 17:58 MSK  
Immutable source: `origin/feat/pwa-public` at `fc2b7dd`  
Mode: `superpowers-lite` — read-only review and orchestration documentation; no product implementation, DB apply, deploy, PR, or merge  
Verdict: **CHANGES REQUIRED — G1 candidate is not ready**  
Test-DB verdict: **HOLD — do not apply T1 migrations or fixtures**

## Independent verification

- Immutable pin: branch HEAD and `origin/feat/pwa-public` both equal `fc2b7dd`. The only local delta is a later one-line edit in `T1-REPORT.md`; it was excluded from immutable evidence.
- `npm test`: PASS, 17/17.
- `npm run build`: PASS; Vite reports a 578.74 kB JavaScript chunk warning.
- `npm run browser-smoke`: PASS, 14/14.
- `npm run lint`: FAIL, 9 errors + 2 warnings. The pushed report says 11 errors + 3 warnings, so its count is not reproducible.
- Redacted environment preflight: `.env.test.local` contains the four `TEST_SUPABASE_*` variables and its URL hostname matches `TEST_SUPABASE_PROJECT_REF`; no values were printed.
- `npm run smoke` against that approved test target: FAIL, 3/9. All six required fixtures/paths are absent.
- Review size: 4,193 changed lines across 39 files versus `origin/main`; about 2,555 product/config/style lines remain after generated/report/test exclusions.

## Blocking findings

### 1. BLOCKER — DB harness neither provisions deterministic fixtures nor enforces the test-project boundary

The accepted DB gate requires an idempotent fixture setup with stable IDs, a fail-closed URL-host check against `TEST_SUPABASE_PROJECT_REF`, and exclusive use of `TEST_SUPABASE_*` variables. The immutable tip does not implement those requirements.

Evidence:

- `scripts/smoke-public.mjs:70-147` only discovers pre-existing arbitrary rows; no T1 file creates the required gray published, colored published, pending, hidden, facade objects, or photo rows.
- `scripts/smoke-public.mjs:156-161` falls back from test variables to `SUPABASE_*`, `VITE_SUPABASE_*`, and `SMOKE_SERVICE_ROLE_KEY`.
- `TEST_SUPABASE_PROJECT_REF` is never read and the URL hostname is never validated.
- The actual test-target run found none of the six required fixtures/paths and exited 1 with 3/9.

Required: add a deterministic, idempotent fixture setup/teardown or stable retained fixture set, remove all non-test fallbacks, fail closed unless the hostname matches the explicit test project ref, then rerun the full anon positive/negative DB and Storage proof. Until that artifact is re-reviewed, **DB apply remains HOLD**.

### 2. P1 — the public PWA replaces the existing Telegram Mini App entrypoint

`src/App.tsx:6` now renders only `WelcomeLegend`/`PublicMap`, removing the auth flow, add wizard, profile, admin panel, and Telegram integration. `index.html:11` removes the Telegram WebApp SDK. The repository still has one entrypoint, while `docs/backend-contract-2026-06-01.md:58-72` continues to define the Mini App client flow.

Merging/deploying this artifact at the current application URL would therefore remove an existing product surface. Required: add an explicit route/hostname/build entry that preserves the Mini App, or obtain and record an Arbiter contract decision that replacement is intentional. The backend contract itself must not be changed without Alkhas.

### 3. P1 — checked-in environment contract cannot configure the new client

`src/services/supabase.ts:3-14` requires `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`, but `.env.example:1-3` documents only the server-side names. A fresh worktree/deployment configured from the repository contract can build and then show only the configuration error.

Required: document the two public client variable names (names only, never values) and retain separate server-only names.

### 4. HIGH — the core populated-map integration remains unproved

The browser smoke declares success after shell/menu checks and does not require data load, a marker, place-sheet open, or a filter changing visible places. Unit tests cover row mapping and pure filtering, not the asynchronous integration starting at `src/components/PublicMap.tsx:75`.

Required: add a component or browser integration with controlled Supabase/route fixtures covering success, partial-photo, error/retry, marker selection, sheet, and visible filtering. This is separate from the external screenshot/device gate, which also remains closed.

### 5. HIGH — the immutable review unit is too large for a reliable single acceptance step

The branch changes 4,193 lines in 39 files. Even after generated/report/test exclusions, roughly 2,555 product/config/style lines remain. The 2,300-line UI block mixes PWA plumbing, controls/overlays, integration, and CSS.

Required for the correction round: keep coherent immutable blocks and give SOL a focused SHA list; isolate PWA/config, controls/overlays, and data integration/tests so regressions can be reviewed independently. No history rewrite or force-push is authorized.

## Additional required corrections and residual gates

### 6. MEDIUM — claimed Tab/focus coverage is not present

`T1-REPORT.md:27` claims browser `Tab/Enter/Escape`, but `scripts/browser-smoke.mjs:84+` uses Enter and Escape only; `src/utils/focusTrap.test.ts:15+` checks Escape only. Add forward-Tab and Shift-Tab wrapping for menu, filters, and place sheet, including focus return.

### 7. MEDIUM — filter-close behavior is named but not asserted

`src/components/MapFilters.test.tsx:7-25` names an apply-and-close test but asserts only `onApplyStatus`; `onClose` and parent focus restoration are not tested. Cover this at `PublicMap` integration level.

### 8. P2 — public data load is unbounded

`src/services/places.ts:176-180` fetches the entire published corpus without `range` or `limit`, and `src/components/PublicMap.tsx:89` stores it wholesale. Add a bounded/paginated or viewport-based loading contract before corpus growth can turn the map into a memory/network failure.

### 9. PROCESS — pushed handoff evidence contains factual drift

The pushed report's lint count is not reproduced, and its push-tip line still names an earlier tip; the local one-line correction is uncommitted and cannot count as evidence. The correction handoff must report only machine-reproduced results at the new immutable tip.

### 10. EXTERNAL GATES — visual/device and release authority remain closed

The browser smoke is not the Antigravity screenshot review or Alkhas physical-device check. Vercel deploy, Supabase apply, secrets, PR, and `main` merge remain explicit Arbiter gates.

## Prior-review disposition

- Fixed in code: browser zoom restrictions removed; visible tile attribution restored; language switch removed for Russian-only sprint; geolocation moved behind the explicit “Найти меня” action; immutable commits are pushed.
- Improved but not fully proved: keyboard naming/focus logic and browser checks.
- Still blocking: deterministic fixture-backed DB/Storage proof and external visual/device evidence.

## Re-review minimum

1. New pushed immutable SHA list with a clean implementation worktree and factual report.
2. DB harness meeting every `briefs/T1-TESTDB-GATE-2026-07-15.md` fail-closed/fixture requirement; then a new SOL HOLD/GO decision before any apply.
3. Explicit product decision or separate entrypoint preserving the Telegram Mini App.
4. Correct `.env.example` variable-name contract.
5. Populated-map/filter/sheet integration plus real Tab/Shift-Tab/focus-return coverage.
6. Bounded public-data loading.
7. Antigravity screenshots and Alkhas device verdict before G1 release.

Do not apply migrations/fixtures, deploy, configure secrets/webhooks, open/merge a PR, or merge to `main` without the relevant Arbiter gate.
